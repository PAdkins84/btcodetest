export const NewsFeed = props => (
    <div className="card-list">
        <h1>news feed</h1>
        {/* {this.props.newsResults.map(news => (
                <p>{news.email}</p>
            ))} */}
    </div>
);